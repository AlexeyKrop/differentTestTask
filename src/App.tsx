import {useEffect} from "react";
import {Login} from "./Login";

export const App = () => {
  useEffect(() => {
    // authAPI.me()
    //   .then(res => console.log(res.data))
  })
  return(
    <div>
      <Login/>
    </div>
  )
}

